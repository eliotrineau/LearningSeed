{% extends 'base.html.twig' %}

{% block title %}Modifier le profil{% endblock %}

{% block body %}
    <div class="flex justify-center">
        <h1 class="font-bold text-white rounded-xl py-1 my-4 px-2 bg-[#163853]">Modifier votre profil</h1>
    </div>
    <div class="flex justify-center mb-4">
        <div class="bg-[#163853] rounded-xl py-1 px-2">
            {{ include('profil/_form.html.twig', {'button_label': 'Modifier'}) }}
        </div>
    </div>
    <div class="flex justify-center">
        <a class="font-bold rounded-xl py-1 px-2 bg-[#163853] text-white" href="{{ path('app_profil_show') }}">Retour au profil</a>
    </div>
{% endblock %}

